import NavBar from "./Components/NavBar"; // Your updated Tabs component

export default function App() {
    return (
        <div
            className={`min-h-dvh transition-colors duration-300 antialiased  text-foreground"}`}>
            <NavBar />
        </div>
    );
}
